
# PR Formatting Preference
When generating Pull Request descriptions, format them plainly so they can be easily copy-pasted into GitHub. Avoid wrapping the entire PR template inside a code block or placing markdown ticks around it if it makes it difficult for the user to copy. Ensure it strictly matches the project's PR template format.
